# Data Matters v3.1 修改報告

## 1. 職涯探索結果

- 保留中文真實職能名稱。
- 新增英文職能名稱 `en_name`。
- Hero 角色圖放大至桌面最高 440px、手機最高 280–310px。
- 圖片容器維持 1:1；由 `object-fit: cover` 改成 `contain`，避免裁切。

## 2. 分享探索結果

- 角色圖由 600px 放大為 720px 正方形。
- 以 `contain` 完整顯示角色。
- 移除 QR Code 與 `vendor/qrcode.js` 載入。
- 移除分享面板中的 Instagram 限制文字。
- 保留下載 PNG、Web Share 與複製 referral URL。

## 3. 職涯圖鑑

- 改為一次聚焦一張角色卡的橫向牌組。
- 支援觸控左右滑動、左右按鈕、鍵盤方向鍵與頁面圓點。
- 桌面卡片採圖片＋文字雙欄；手機改為上下排列。

## 4. 你的工作重心

- 重建座標圖 Grid，Y 軸標題不再與上下說明重疊。
- 完整顯示 X／Y 軸兩端說明。
- 四象限標籤改為圖內獨立元素，不再使用空白字串排版。

## 5. 留言板

- 移除分類 Chips。
- 移除發文表單的分類問題。
- 留言卡不再顯示分類。
- 讀取 API 不再接受分類篩選。
- 後端統一將新貼文寫入 `category = 一般討論`，保留資料庫欄位以相容既有資料。
- 已有 Supabase 表需執行 `004_simplify_community_category.sql`。
