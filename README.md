# Data Matters v3.10 — Analytics Event Registry

This update converts Product Analytics event names to a single source of truth without changing UI, recommendation scoring, event payload structure, sharing, or community behavior.

Start with `APPLY_UPDATE.md`. Supabase deployment steps are in `SUPABASE_DEPLOYMENT.md`.
